import 'package:flutter/material.dart';

class PriceEstimatorScreen extends StatelessWidget {
  const PriceEstimatorScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('Price Estimator')));
  }
}
